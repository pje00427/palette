<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.palette.product.domain.artist.mapper.ArtistMapper">

    <!-- 활성화된 아티스트 전체 조회 -->
    <select id="findAllActive" resultType="ArtistResponse">
        SELECT id,
               name,
               bio,
               profile_image,
               instagram_url,
               is_active,
               created_at
        FROM artists
        WHERE is_active = 1
          AND deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

    <!-- 아티스트 단건 조회 -->
    <select id="findById" resultType="ArtistResponse">
        SELECT id,
               name,
               bio,
               profile_image,
               instagram_url,
               is_active,
               created_at
        FROM artists
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

</mapper>